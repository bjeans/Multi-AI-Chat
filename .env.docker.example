# Docker Environment Configuration for LLM Council

# LiteLLM Proxy Configuration
# If LiteLLM is running on your host machine, use:
#   - macOS/Windows: host.docker.internal:4000
#   - Linux: 172.17.0.1:4000 or your host's IP address
LITELLM_PROXY_URL=http://host.docker.internal:4000

# Your LiteLLM API Key (REQUIRED - replace with actual key)
# The application will fail to start without a valid API key
LITELLM_API_KEY=sk-your-actual-api-key-here

# CORS Origins - Controls which domains can access the API
# SECURITY: In production, ALWAYS set this to your specific domain(s)
# Development (local): http://localhost:8000
# Production: https://yourdomain.com,https://app.yourdomain.com
# Default if not set: http://localhost:8000
CORS_ORIGINS=http://localhost:8000

# Note: DATABASE_URL is set automatically in docker-compose.yml
# to use the mounted volume at /app/data/database.db
